% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-environment.R
\name{validate_sim_period}
\alias{validate_sim_period}
\title{Validates Simulation Period}
\usage{
validate_sim_period(yr, day)
}
\arguments{
\item{yr}{The simulation year.}

\item{day}{A vector of length 4 containing start and end
day (day, month) as numeric values.}
}
\value{
None. The function exits normally if validation passes,
and throws an error if validation fails.
}
\description{
Validates the provided start and end dates
for a simulation period.
}
\details{
This function checks if the combination of \code{yr} and \code{day} (start
and end day) can be successfully converted to valid ISO dates
using \code{ISOdate}. If either conversion fails (resulting in NA), the
function stops with an error message.
}
\examples{
\dontrun{
# Valid period
validate_sim_period(2024, c(1, 1, 12, 31))

# Invalid period (February with 31 days)
validate_sim_period(2024, c(1, 2, 2, 31))
}
}
